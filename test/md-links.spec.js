import { mdLinks } from "..";

describe('mdLinks', () => {

  it('should...', () => {
    console.log('FIX ME!');
  });
  it('Debería devolver una promesa',()=>{
    expect(mdLinks()).toBe(typeof Promise);
  })

});
